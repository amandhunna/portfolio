# Verification (HTML vs React)

**Single source of truth:** **docs/ai/verification.md**

That file lists what is implemented vs missing when comparing the original HTML portfolio to the React app. Both Claude and Cursor read from docs/ai/.
